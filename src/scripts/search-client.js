import { initSearch } from './search.js';

// Export a function that can be called with data
export function initSearchWithData(posts, baseUrl, currentLang) {
  initSearch(posts, baseUrl, currentLang);
}
